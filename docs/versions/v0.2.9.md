# v0.2.9

날짜: 2026-05-09
범위: 프론트엔드

## 발견된 이슈

1. v0.2.8에서 `?` 도움말 버튼을 `bottom-20`(80px)으로 올렸는데, 단어 방의 fixed 컴포저 바(높이 ~88px, 상단에 dashed 구분선)와 여전히 부분적으로 겹쳐 위치가 애매함. 다른 페이지(홈/`/me`/`/me/notifications`)에서는 굳이 그렇게 높이 띄울 필요가 없어 코너에 두는 게 자연스러움

## TODO

- [x] `HelpFloatingButton`에 `usePathname` 기반 분기 추가
  - 단어 방(`/words/...`): `bottom-[100px]` — 컴포저 바 위로 명확히 띄움
  - 그 외 페이지: `bottom-6` — 기존 코너 위치로 환원
- [x] 버전 인덱스에 v0.2.9 추가

## 변경 파일

- `frontend/src/components/ui/HelpFloatingButton.tsx` — `usePathname` import + `aboveComposer` 분기, `bottom-[100px]` / `bottom-6` 양쪽
- `docs/versions/v0.2.9.md` (이 문서) + `docs/versions/README.md` 인덱스

## 빌드

- 프론트 `npm run build` 통과 확인

## 사용자 영향

- 단어 방: `?` 버튼이 컴포저 바 dashed 구분선 위로 명확히 떠서, 쓰기 입력 영역과 시각적으로 분리됨
- 홈/`/me` 등: `?`가 다시 우하단 코너로 돌아옴 (v0.2.8 이전 위치)

## 알려진 잔존

- 단어 방 시점에서 `?`는 fixed 위치라 길게 누르거나 컴포저가 두 줄로 늘어나는 등 동적 변화에 자동으로 따라가지 않음. 일단 보통 케이스에서는 충분
