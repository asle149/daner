# v0.2.18

날짜: 2026-05-10
범위: 프론트엔드

## 발견된 이슈

1. v0.2.17 에서 헤더를 sticky 로 만들면서 `bg-background/90 backdrop-blur` 를 입혔는데, body 의 `background.png` 톤보다 헤더 배경(거의 흰색)이 밝아 "DANER" 영역만 색이 떠 보이는 이질감이 생김

## TODO

- [x] `Header.tsx`: `bg-background/90` 제거하고 `backdrop-blur` → `backdrop-blur-sm`. body 의 배경 PNG (fixed) 가 헤더 영역에서도 그대로 비치게 됨

## 변경 파일

- `frontend/src/components/ui/Header.tsx`

## 빌드

- 프론트 `npx next build` 통과

## 사용자 영향

- 모든 페이지 상단 헤더가 body 배경 PNG 와 같은 톤으로 보임
- 스크롤 중 콘텐츠가 헤더 뒤로 지나가도 약한 blur 로 가독성은 유지

## 알려진 잔존

- 헤더 위로 글자 색이 어두운 콘텐츠가 지나가면 잠깐 겹쳐 보일 수 있음. 그래도 글자/아이콘 영역이 좁아 실질적으로 거슬리진 않음 — 추후 필요 시 매우 옅은 톤(2~5%) 다시 검토
