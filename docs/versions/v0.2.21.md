# v0.2.21

날짜: 2026-05-12
범위: 프론트엔드

## 발견된 이슈

1. 탭 제목이 `"다너 (Daner) — 단어로 모이는 작은 커뮤니티"` 라 너무 길고, 페이지별 템플릿도 `%s · 다너` 형식이라 한글/특수문자 섞여 어수선
2. favicon 이 Next.js 기본 (검은 삼각형). 사이트 톤(Memoment Kkukkuk 폰트, 무채색)과 안 어울림

## TODO

- [x] `app/layout.tsx` metadata
  - `title.default`: `"다너 (Daner) — 단어로 모이는 작은 커뮤니티"` → `"DANER"`
  - `title.template`: `"%s · 다너"` → `"%s | DANER"`
  - openGraph / twitter `title`, `siteName` 도 `"DANER"` 로 통일
- [x] `app/icon.tsx` 신규 — Next.js 16 의 동적 아이콘 라우트
  - 64×64 PNG 를 빌드 시 자동 생성 (`/icon` 라우트로 노출, favicon.ico 보다 우선 적용)
  - `next/og` 의 `ImageResponse` 사용
  - `public/fonts/MemomentKkukkukk.ttf` 를 빌드 시점에 `node:fs/promises.readFile` 로 읽어서 폰트 임베드
  - 배경 `--background` 톤 (#fdfcfa), 글자 `--foreground` (#1a1a1a), `letterSpacing: -0.02em` 으로 사이트 본문이랑 같은 톤
  - 한 글자 "D"

## 변경 파일

- `frontend/src/app/layout.tsx`
- `frontend/src/app/icon.tsx` (신규)

## 빌드

- 프론트 `npx next build` 통과. 새 라우트 `/icon` 이 자동 등록됨 (Static prerendered)

## 사용자 영향

- 모든 탭이 `DANER` 또는 `{페이지명} | DANER` 형식으로 깔끔해짐
- 브라우저 탭 / 즐겨찾기 / 모바일 홈 화면 추가 시 사이트 본문과 같은 폰트의 "D" 가 보임
- OG 카드 / 트위터 카드 미리보기도 "DANER" 단일 표기

## 알려진 잔존

- 페이지 단위 metadata(예: 단어 방의 단어 자체를 title 로 노출) 는 아직 안 함 — `app/words/[word]/page.tsx` 가 `'use client'` 라 `generateMetadata` 추가가 별도 작업. 추후 v3
- apple-icon (iOS 홈 화면용 180×180) 은 아직 — 필요해지면 `app/apple-icon.tsx` 추가하면 됨
