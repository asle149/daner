# v0.2.7

날짜: 2026-05-09
범위: 프론트엔드

## 발견된 이슈

1. v0.2.6에서 도입한 타자기 1회 한정(sessionStorage) 동작이 의도와 다름 — 매번 재생되도록 되돌려야 함

## TODO

- [x] `Typewriter`에서 `onceKey` prop 제거, sessionStorage 로직 제거. 컴포넌트 마운트마다 항상 처음부터 재생
- [x] 홈(`page.tsx`)의 `<Typewriter onceKey="home-greeting" />` → `<Typewriter />`

## 변경 파일

- `frontend/src/components/ui/Typewriter.tsx` — `onceKey`, sessionStorage 분기, 관련 useEffect 삭제. unused `CHO_COUNT`/`JUNG` 상수도 정리
- `frontend/src/app/page.tsx` — `onceKey` 인자 제거

## 빌드

- 프론트 `npm run build` 성공

## 사용자 영향

- 홈에 진입할 때마다 "오늘의 단어는?"이 다시 타이핑되며 커서 깜빡 — v0.2.5 이전 동작과 동일

## 알려진 잔존

없음 (관련 변경 한 줄짜리 되돌림)
